source "https://rubygems.org"

# GitHub Pages compatible gems (for local development when possible)
gem "github-pages", "~> 232", group: :jekyll_plugins
gem "webrick", "~> 1.8"

# Note: The minimal-mistakes theme is loaded via remote_theme in _config.yml
# This allows GitHub Pages to build the site even if local bundler fails
